import React, { FC } from "react";
import { DrawerMenu } from "@components";

const App: FC = () => {
  return (
    <div style={{ height: "100vh", width: "100%" }}>
      <DrawerMenu />
    </div>
  );
};

export default App;
